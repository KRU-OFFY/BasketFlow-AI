# AI

## Environment diagnostics

Use the included read-only diagnostic script to verify the repository workstation, CLI tools, Codex MCP setup, GitHub CLI, VS Code CLI, and network connectivity:

```bash
./scripts/check_environment.sh
```

The latest container-specific findings and remediation notes are documented in [`docs/tooling-status.md`](docs/tooling-status.md).
