import { AppShell } from '@/components/layout/app-shell'; import { Card } from '@/components/ui/card'; import { mockScript } from '@/lib/ai/script-generator';
export default function Script(){const s=mockScript({title:'ตัวอย่างสินค้า',duration:30});return <AppShell><h1 className="text-3xl font-bold">Script Studio</h1><Card className="mt-6"><label className="label">Duration</label><select className="input mt-1"><option>15 seconds</option><option>30 seconds</option><option>60 seconds</option><option>90 seconds</option></select><pre className="mt-4 whitespace-pre-wrap text-sm">{JSON.stringify(s,null,2)}</pre></Card></AppShell>}
