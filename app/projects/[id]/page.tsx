import Link from 'next/link'; import { AppShell } from '@/components/layout/app-shell'; import { Card } from '@/components/ui/card'; import { WorkflowStepper } from '@/components/projects/stepper';
export default function Project({params}:{params:{id:string}}){const links=['brief','script','compliance','media','approval'];return <AppShell><h1 className="text-3xl font-bold">โปรเจกต์ {params.id}</h1><div className="mt-4"><WorkflowStepper current="product_imported"/></div><div className="mt-6 grid gap-4 md:grid-cols-5">{links.map(l=><Link href={`/projects/${params.id}/${l}`} key={l}><Card className="hover:border-orange">{l}</Card></Link>)}</div></AppShell>}
