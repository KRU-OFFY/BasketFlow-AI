import { callAiJson } from './call-ai-json'; import { scriptGeneratorPrompt } from './prompts/script-generator'; import { THAI_AFFILIATE_DISCLOSURE, THAI_LIMITATION_STATEMENT } from './constants';
export { THAI_AFFILIATE_DISCLOSURE } from './constants';
export type ScriptInput = {title:string; duration:number; risk_flags?:string[]};
export function mockScript(input:ScriptInput) { const limitation=input.risk_flags?.length ? ` ${THAI_LIMITATION_STATEMENT}` : ''; const full = `ก่อนซื้อ ${input.title} มาดูจุดเด่นและข้อควรรู้กันแบบตรงไปตรงมา สินค้านี้เหมาะกับคนที่ต้องการตัวช่วยในชีวิตประจำวัน ลองดูรายละเอียด ราคา และรีวิวผู้ใช้เพิ่มเติมก่อนตัดสินใจ${limitation} ${THAI_AFFILIATE_DISCLOSURE}`; return { hook:`ก่อนซื้อ ${input.title} ดูคลิปนี้ก่อน`, problem:'ผู้ซื้ออยากรู้ว่าสินค้านี้เหมาะกับตนเองหรือไม่', product_intro:`นี่คือ ${input.title}`, benefits:['นำเสนอคุณสมบัติหลัก','ช่วยให้ตัดสินใจง่ายขึ้น'], use_case:'เหมาะสำหรับการใช้งานทั่วไปตามคำแนะนำของผู้ขาย', cta:'ดูรายละเอียดสินค้าได้ที่ลิงก์ในคำอธิบาย', affiliate_disclosure:THAI_AFFILIATE_DISCLOSURE, full_script:full, subtitle_lines:full.split(' ').reduce<string[]>((a,w,i)=>{ const idx=Math.floor(i/8); a[idx]=`${a[idx]??''} ${w}`.trim(); return a;},[]) }; }
export async function generateReviewScript(input:ScriptInput) { return callAiJson(scriptGeneratorPrompt(input), () => mockScript(input)); }
